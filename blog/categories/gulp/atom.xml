<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">

  <title><![CDATA[Category: gulp | kaku's Blog]]></title>
  <link href="http://kaku87.github.com/blog/categories/gulp/atom.xml" rel="self"/>
  <link href="http://kaku87.github.com/"/>
  <updated>2015-01-11T13:31:19+09:00</updated>
  <id>http://kaku87.github.com/</id>
  <author>
    <name><![CDATA[kaku]]></name>
    
  </author>
  <generator uri="http://octopress.org/">Octopress</generator>

  
  <entry>
    <title type="html"><![CDATA[gulp入门]]></title>
    <link href="http://kaku87.github.com/blog/2015/01/11/about-gulp/"/>
    <updated>2015-01-11T01:05:00+09:00</updated>
    <id>http://kaku87.github.com/blog/2015/01/11/about-gulp</id>
    <content type="html"><![CDATA[<h1>gulp.js</h1>

<h2>概要</h2>

<p>前端任务自动化工具</p>

<h2>官方网站</h2>

<p><a href="gulpjs.com">gulpjs.com</a></p>

<h2>相关知识</h2>

<p>GoogleのWenStarterKit是用gulp来做的。</p>

<h2>环境</h2>

<p>vagrant centos</p>

<h2>安装</h2>

<pre><code>1. sudo yum -y install epel-release

2. sodu yum -y install nodejs npm

    `node -v`确认node的版本号

    `npm -v`确认npm的版本号

3. sudo npm install gulp -g

    `gulp -v`确认gulp的版本号
</code></pre>

<h2>gulp和grunt的区别</h2>

<table>
<thead>
<tr>
<th>gulp         </th>
<th> grunt </th>
</tr>
</thead>
<tbody>
<tr>
<td> 插件少       </td>
<td>  插件多</td>
</tr>
<tr>
<td>8,398sta     </td>
<td>  8,439starM</td>
</tr>
<tr>
<td>2013/6/30    </td>
<td>  2011/9/18</td>
</tr>
<tr>
<td>gulpfie.js   </td>
<td>  Gruntfile.js</td>
</tr>
<tr>
<td>类似Node       </td>
<td>类似JavaScript</td>
</tr>
<tr>
<td>Node的插件    </td>
<td>  Grunt的插件</td>
</tr>
</tbody>
</table>


<h2>和Grunt相比较的优点</h2>

<p>1.配置文件比Grant要少</p>

<p>2.比Grant要快</p>

<h2>和Grant相比的缺点</h2>

<p>1.写法和Node接近，所以相对比较复杂</p>

<p>2.文档较少</p>

<h2>创建package.json</h2>

<pre><code>mkdir mysite
cd mysite 
npm init
npm install --save-dev gulp
npm -i -D gulp
rm -rf node_modules
npm install
</code></pre>

<h2>初识gulp.task()</h2>

<pre><code>var gulp = require('gulp');
gulp.task('hello', function(){
    console.log('hello world!')
})

gulp.task('default', ['hello']);
</code></pre>

<h2>文件拷贝</h2>

<p>把当前目录里的package.json文件拷贝到当前目录中的dist文件夹中。</p>

<pre><code>var gulp = require('gulp');

gulp.task('copy', function() {
    gulp.src('./package.json')
      .pipe(gulp.dest('./dist'));
});

gulp.task('default',['copy']);
</code></pre>

<h2>gulp的常用5种API</h2>

<ol>
<li><p>定义task</p>

<pre><code> gulp.task('name',['tasks'], function() {
     // content
 });
</code></pre></li>
<li><p>输入路径指定</p>

<pre><code> gulp.src('files')
   .pipe(name(''))
</code></pre></li>
<li><p>执行task</p>

<pre><code> gulp.task('foo', function() {
     gulp.run('bar');
 });
</code></pre></li>
<li><p>输出路径指定</p>

<pre><code> .pipe(gulp.dest('folder'));
</code></pre></li>
<li><p>监视文件</p>

<pre><code> gulp.watch('files', function (event) {
     console.log('js file changed!!');
 });
</code></pre></li>
</ol>


<p>详细API信息请参照这里   <a href="https://github.com/gulpjs/gulp/blob/master/docs/API.md">github API doc</a></p>

<h2>gulp task执行顺序</h2>

<p>gulp的task默认为并行触发，若要顺序执行，要做如下处理。</p>

<ol>
<li>前面的task要写return</li>
<li><p>后面的task要加第二参数，值为前面的task</p>

<pre><code> var gulp = require('gulp');

 gulp.task('first', function() {
     return gulp.src('./package.json')
       .pipe(gulp.dest('./dist'));
 });

 gulp.task('second',['first'], function() {
     console.log('first task done!!');
 });

 gulp.task('default',['second']);
</code></pre></li>
</ol>

]]></content>
  </entry>
  
</feed>
